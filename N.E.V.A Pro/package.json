{
  "name": "enem-gamification-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "npm run install-all",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "install-all": "npm install --prefix frontend && npm install --prefix backend && npm run prisma:generate --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
